# Example: the workflow CI Shard Advisor generates for the sample run
# (`ci-shard-advisor samples/cypress-containers/*.json --setup 45s --format github`).
# Each parallel job runs EXACTLY its duration-optimal spec list and keeps its
# own mochawesome report: the input for the next analysis. (Lives under
# examples/ so it does NOT run in this repo.)
# Generated by CI Shard Advisor. Each job runs its duration-optimal spec list.
name: Tests (optimal split)
on:
  push:
    branches: [main]
  pull_request:
jobs:
  shard-1:
    name: Container 1/5 (optimal split)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Run this shard's specs
        run: npx cypress run --reporter mochawesome --reporter-options "reportDir=reports,overwrite=false,html=false,json=true" --spec "cypress/e2e/admin/products.cy.ts,cypress/e2e/checkout/guest-checkout.cy.ts"
      - name: Upload this shard's report (feed all of them back to the advisor)
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: shard-1
          path: reports/
          retention-days: 7
  shard-2:
    name: Container 2/5 (optimal split)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Run this shard's specs
        run: npx cypress run --reporter mochawesome --reporter-options "reportDir=reports,overwrite=false,html=false,json=true" --spec "cypress/e2e/admin/orders.cy.ts,cypress/e2e/checkout/logged-in-checkout.cy.ts,cypress/e2e/checkout/payment-methods.cy.ts"
      - name: Upload this shard's report (feed all of them back to the advisor)
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: shard-2
          path: reports/
          retention-days: 7
  shard-3:
    name: Container 3/5 (optimal split)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Run this shard's specs
        run: npx cypress run --reporter mochawesome --reporter-options "reportDir=reports,overwrite=false,html=false,json=true" --spec "cypress/e2e/admin/refunds.cy.ts,cypress/e2e/auth/login.cy.ts,cypress/e2e/auth/signup.cy.ts,cypress/e2e/checkout/promo-codes.cy.ts,cypress/e2e/checkout/shipping-options.cy.ts"
      - name: Upload this shard's report (feed all of them back to the advisor)
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: shard-3
          path: reports/
          retention-days: 7
  shard-4:
    name: Container 4/5 (optimal split)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Run this shard's specs
        run: npx cypress run --reporter mochawesome --reporter-options "reportDir=reports,overwrite=false,html=false,json=true" --spec "cypress/e2e/account/addresses.cy.ts,cypress/e2e/admin/inventory.cy.ts,cypress/e2e/cart/cart.cy.ts,cypress/e2e/catalog/product-page.cy.ts,cypress/e2e/catalog/search.cy.ts,cypress/e2e/returns/returns.cy.ts"
      - name: Upload this shard's report (feed all of them back to the advisor)
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: shard-4
          path: reports/
          retention-days: 7
  shard-5:
    name: Container 5/5 (optimal split)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Run this shard's specs
        run: npx cypress run --reporter mochawesome --reporter-options "reportDir=reports,overwrite=false,html=false,json=true" --spec "cypress/e2e/a11y/smoke.cy.ts,cypress/e2e/account/order-history.cy.ts,cypress/e2e/account/profile.cy.ts,cypress/e2e/account/wishlist.cy.ts,cypress/e2e/auth/password-reset.cy.ts,cypress/e2e/catalog/filters.cy.ts,cypress/e2e/i18n/locales.cy.ts,cypress/e2e/notifications/emails.cy.ts"
      - name: Upload this shard's report (feed all of them back to the advisor)
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: shard-5
          path: reports/
          retention-days: 7

