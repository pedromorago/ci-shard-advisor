{
  "name": "@ci-shard-advisor/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --reporter=default --reporter=junit --outputFile.junit=reports/junit/results.xml",
    "test:mutation": "stryker run",
    "typecheck": "tsc --noEmit",
    "test:junit": "vitest run --reporter=junit --outputFile=reports/junit/results.xml"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@vitest/coverage-v8": "^3.2.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
